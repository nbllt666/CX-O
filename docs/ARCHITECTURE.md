# CX-O 系统架构文档

## 目录

- [1. 系统总览](#1-系统总览)
- [2. 子项目概览](#2-子项目概览)
- [3. 系统总览架构图](#3-系统总览架构图)
- [4. 后端核心模块依赖图](#4-后端核心模块依赖图)
- [5. 数据流图](#5-数据流图)
- [6. WebSocket 通信架构](#6-websocket-通信架构)
- [7. 技术栈](#7-技术栈)

---

## 1. 系统总览

CX-O 是一个 AI 虚拟主播/助手平台，采用前后端分离的微服务架构，由五个核心子项目组成：

| 子项目 | 技术栈 | 端口 | 职责 |
|--------|--------|------|------|
| CX-O-SERVER | Python / FastAPI | 8000 | 核心后端（含网关、ASR、TTS） |
| CX-O-Gateway | Python / FastAPI | 8100 | 轻量级独立网关（可选部署） |
| CX-O-VoiceWorkStation | Python / FastAPI | 8200 | 语音工作站（训练/微调/推理） |
| CX-O-Frontend | React / TypeScript | 5173 | 前端界面 |
| Orpheus TTS | Python / FastAPI | 5060 | 流式情感语音合成（vLLM + SNAC 解码） |

CX-O-SERVER 采用单体架构整合了网关、后端业务、ASR、TTS 四大模块，通过 `lifespan` 管理所有服务的生命周期。CX-O-Gateway 可在需要独立部署网关时使用。

---

## 2. 子项目概览

### 2.1 CX-O-SERVER

核心后端服务，整合以下模块：

**核心模块 (`server/core/`)**

| 模块 | 职责 |
|------|------|
| `acp/` | ACP 多智能体协作协议 — UDP 广播发现、Agent 注册、群组管理 |
| `alarm/` | 闹钟/定时提醒 — SQLite 持久化、`threading.Timer` 调度 |
| `cache.py` | LRU 缓存 — TTL 过期策略、命中率统计 |
| `context/` | 对话上下文管理 — Mono 临时上下文、LRU 缓存、摘要压缩 |
| `cxfc/` | CXFC 插件联邦协议 — 插件发现、心跳检测、Skill 注册与模板渲染 |
| `graph/` | 知识图谱 — SQLite + Weaviate 存储、四类图谱（实体/关系/事件/概念）、图算法 |
| `llm/` | LLM 客户端 — Ollama/vLLM 后端适配、工具调用 |
| `memory/` | 智能记忆系统 — 衰减模型、混合搜索（向量+关键词）、场景路由、去重 |
| `memory/secondary_router.py` | 副模型路由 — 记忆管理副模型命令分发 |
| `memory/decay_batch.py` | 批量衰减处理器 — 定期批量更新记忆衰减值 |
| `memory/graph_store.py` | 图存储桥接 — 记忆系统与知识图谱的桥接层 |
| `memory/milvus_lite_store.py` | Milvus Lite 向量存储 — 轻量级本地向量数据库 |
| `memory/chroma_store.py` | ChromaDB 向量存储 — 本地向量数据库备选方案 |
| `model_router.py` | 多模型路由 — main/summary/memory 三角色路由、跟随机制、预热 |
| `plugins/` | 插件系统 — 生命周期管理、Hook 机制、API 注入 |
| `session/` | 会话持久化 — SQLite 存储、自动清理 |
| `tools/` | 工具注册表 — 内置/MCP/CXFC/自定义工具、OpenAI Function 格式 |
| `websocket/` | WebSocket 管理器 — 频道订阅/发布、连接管理、离线回调 |

**业务服务 (`server/services/`)**

| 服务 | 职责 |
|------|------|
| `asr_service.py` | 语音识别 — embedded（SenseVoice）/ remote 双模式 |
| `tts_service.py` | 语音合成 — 情感语音、音效插入、流式合成 |
| `vad_processor.py` | 语音活动检测 — Energy / WebRTC / Silero 三模式 |
| `interrupt_manager.py` | 全双工打断 — 用户打断 Agent + Agent 打断用户 |
| `emotion_parser.py` | 情感解析 — 15 种情感标记 |
| `effect_parser.py` | 音效解析 |
| `prompt_builder.py` | 提示词构建 |
| `hidden_prompt.py` | 隐藏提示词注入 |
| `live_client.py` | 直播客户端处理 |
| `firewall.py` | 三档防火墙 — block / passive / reply |
| `adaptive_polling.py` | 自适应轮询 — 根据负载动态调整轮询间隔 |
| `text_smoother.py` | 文本平滑器 — 流式文本去重和平滑处理 |
| `frontend_marker.py` | 前端标记适配 — 内部标记转前端格式 |
| `marker_adapter.py` | 标记适配器 — 情感/音效/动作标记统一适配 |

**API 层**

| 层 | 职责 |
|----|------|
| `server/api/routers/` | REST API 路由（chat, memory, agents, tools, graph 等） |
| `server/handlers/` | WebSocket 消息处理器（chat, memory, audio, tools, plugin 等） |
| `server/protocol/` | 协议定义 — Action 常量、消息格式 |

### 2.2 CX-O-Gateway

轻量级网关服务，独立部署时使用：

- 反向代理到 CX-O-SERVER
- 独立的 WebSocket 端点（`/ws`, `/ws/live`）
- 防火墙服务
- Control 服务反向代理

### 2.3 CX-O-VoiceWorkStation

语音工作站，提供完整的语音定制工作流：

| 服务 | 职责 |
|------|------|
| F5-TTS | 语音克隆微调 |
| So-VITS-SVC | 语音转换训练/推理 |
| CosyVoice | 情感参考生成（8 情感 + 56 过渡 = 64 参考音频） |
| IndexTTS 2 | 情感控制合成（8 情感 × 5 强度 = 40 组合） |
| VoxCPM | 三模式语音生成（design / controllable_clone / ultimate_clone） |

**五步工作流：**

1. 参考音频生成（VoxCPM）
2. 情感参考音频生成（CosyVoice）
3. 训练数据准备（So-VITS-SVC 预处理）
4. 模型训练（So-VITS-SVC 训练）
5. 推理（So-VITS-SVC 推理）

### 2.4 CX-O-Frontend

React + TypeScript 前端应用：

- **构建工具**：Vite
- **桌面应用**：Electron（electron/ 目录：main.ts, preload.ts, config.ts, store.ts）
- **核心页面**：ChatPage, LivePage, DashboardPage, AgentsPage, MemoriesPage, PetPage（桌面宠物）, AudioTestPage（音频测试）, VoiceWorkstationPage（语音工作站）, GraphDataPage（图谱数据）, VectorDataPage（向量数据）, PluginsPage（插件管理）, LiveSplitPage（直播拆分）
- **虚拟形象**：Live2D（PIXI.js）+ VRM（Three.js）
- **桌面宠物组件**：PetAudioPanel, PetAvatar, PetChat
- **特效组件**：VRMWindField（VRM 风场效果）
- **直播**：LiveStage + OBS 分层输出
- **Hooks**：useAudioStream, useAudioAnalyzer, useLiveWebSocket, usePetMousePassthrough
- **Store**：settingsStore（虚拟形象/布局/限制配置）
- **国际化**：i18n（中文/英文）
- **依赖**：@tanstack/react-query, react-markdown, remark-gfm, electron

---

## 3. 系统总览架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CX-O Platform                               │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    CX-O-Frontend (:5173)                     │   │
│  │  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────────┐  │   │
│  │  │ ChatPage │ │ LivePage │ │DashboardPg│ │ MemoriesPage │  │   │
│  │  └────┬─────┘ └────┬─────┘ └─────┬─────┘ └──────┬───────┘  │   │
│  │       │            │             │               │           │   │
│  │  ┌────┴────────────┴─────────────┴───────────────┴──────┐   │   │
│  │  │              API Client / WebSocket Hooks             │   │   │
│  │  └──────────────────────┬───────────────────────────────┘   │   │
│  │                         │ HTTP / WebSocket                   │   │
│  │  ┌──────────────────────┴───────────────────────────────┐   │   │
│  │  │  Live2D (PIXI.js)  │  VRM (Three.js)  │  OBS Output │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └────────────────────────────┬─────────────────────────────────┘   │
│                               │                                     │
│                    ┌──────────┴──────────┐                          │
│                    │   CX-O-Gateway      │                          │
│                    │     (:8100)         │                          │
│                    │  (可选独立部署)      │                          │
│                    └──────────┬──────────┘                          │
│                               │ 反向代理                             │
│  ┌────────────────────────────┴────────────────────────────────┐   │
│  │                    CX-O-SERVER (:8000)                       │   │
│  │                                                             │   │
│  │  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │   │
│  │  │   Gateway    │  │  REST API    │  │   WebSocket       │  │   │
│  │  │  /ws /ws/live│  │  /api/*      │  │   Manager         │  │   │
│  │  └──────┬──────┘  └──────┬───────┘  └────────┬──────────┘  │   │
│  │         │                │                    │              │   │
│  │  ┌──────┴────────────────┴────────────────────┴──────────┐  │   │
│  │  │                    Core Modules                       │  │   │
│  │  │  ┌──────┐ ┌───────┐ ┌──────┐ ┌──────┐ ┌───────────┐ │  │   │
│  │  │  │ LLM  │ │Memory │ │Context│ │ ACP  │ │  Tools    │ │  │   │
│  │  │  └──┬───┘ └───┬───┘ └──┬───┘ └──┬───┘ └─────┬─────┘ │  │   │
│  │  │     │         │        │        │            │       │  │   │
│  │  │  ┌──┴───┐ ┌───┴──┐ ┌──┴──┐ ┌───┴──┐ ┌──────┴─────┐ │  │   │
│  │  │  │Model │ │Graph │ │Cache│ │CXFC  │ │  Plugins   │ │  │   │
│  │  │  │Router│ │Store │ │     │ │Plugin│ │  Registry  │ │  │   │
│  │  │  └──────┘ └──────┘ └─────┘ └──────┘ └────────────┘ │  │   │
│  │  └─────────────────────────────────────────────────────┘  │   │
│  │                                                           │   │
│  │  ┌─────────────────────────────────────────────────────┐  │   │
│  │  │                  Services                            │  │   │
│  │  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌──────────┐ ┌────────┐  │  │   │
│  │  │  │ ASR │ │ TTS │ │ VAD │ │Interrupt │ │Firewall│  │  │   │
│  │  │  └──┬──┘ └──┬──┘ └──┬──┘ └──────────┘ └────────┘  │  │   │
│  │  └─────┼───────┼───────┼──────────────────────────────┘  │   │
│  └────────┼───────┼───────┼──────────────────────────────────┘   │
│           │       │       │                                         │
│  ┌────────┴───────┴───────┴──────────────────────────────────┐   │
│  │              CX-O-VoiceWorkStation (:8200)                  │   │
│  │                                                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │   │
│  │  │  F5-TTS  │ │So-VITS-  │ │CosyVoice │ │  IndexTTS 2  │  │   │
│  │  │ 微调     │ │SVC 训练  │ │情感参考  │ │  情感合成    │  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │   │
│  │  ┌──────────┐ ┌─────────────────────────────────────────┐  │   │
│  │  │ VoxCPM   │ │           五步工作流编排                  │  │   │
│  │  │ 语音生成 │ │  ref_audio → emotion_refs → train_prep  │  │   │
│  │  └──────────┘ │  → training → inference                  │  │   │
│  │               └─────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │                    External Services                        │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐ │     │
│  │  │ Ollama/  │  │ Weaviate │  │ CosyVoice│  │  MCP      │ │     │
│  │  │ vLLM     │  │ 向量数据库│  │ Server   │  │  Servers  │ │     │
│  │  └──────────┘  └──────────┘  └──────────┘  └───────────┘ │     │
│  │  ┌──────────┐                                             │     │
│  │  │ Orpheus  │                                             │     │
│  │  │ TTS      │                                             │     │
│  │  │(:5060)   │                                             │     │
│  │  └──────────┘                                             │     │
│  └────────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. 后端核心模块依赖图

```
                         ┌─────────────────┐
                         │  model_router   │
                         │ (main/summary/  │
                         │    memory)      │
                         └────────┬────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │   LLM    │ │   LLM    │ │   LLM    │
              │  Client  │ │  Client  │ │  Client  │
              │  (main)  │ │(summary) │ │ (memory) │
              └────┬─────┘ └────┬─────┘ └────┬─────┘
                   │            │             │
         ┌─────────┴───────────┴─────────────┘
         │
         │  依赖
         ▼
  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │   memory/    │────▶│   graph/     │────▶│  Weaviate /  │
  │   manager    │     │   database   │     │  SQLite      │
  │              │     └──────────────┘     └──────────────┘
  │  ┌─────────┐ │
  │  │  decay  │ │     ┌──────────────┐     ┌──────────────┐
  │  │  batch  │ │     │   context/   │────▶│   session/   │
  │  └─────────┘ │     │   manager    │     │   store      │
  │  ┌─────────┐ │     └──────────────┘     └──────┬───────┘
  │  │  hybrid │ │                                   │
  │  │  search │ │                                   │ SQLite
  │  └─────────┘ │                                   │
  │  ┌─────────┐ │     ┌──────────────┐              │
  │  │  router │ │────▶│   cache.py   │              │
  │  └─────────┘ │     │  (LRU+TTL)   │              │
  └──────────────┘     └──────────────┘              │
                                                     │
  ┌──────────────┐     ┌──────────────┐              │
  │   tools/     │────▶│   plugins/   │              │
  │   registry   │     │   manager    │              │
  │              │     └──────┬───────┘              │
  │  ┌─────────┐ │            │                      │
  │  │ builtin │ │     ┌──────┴───────┐              │
  │  ├─────────┤ │     │    cxfc/     │              │
  │  │   mcp   │ │     │   manager    │              │
  │  ├─────────┤ │     │              │              │
  │  │  graph  │ │     │  ┌─────────┐ │              │
  │  │  tools  │ │     │  │discovery│ │              │
  │  ├─────────┤ │     │  ├─────────┤ │              │
  │  │ master  │ │     │  │  skill  │ │              │
  │  │  tools  │ │     │  │registry │ │              │
  │  ├─────────┤ │     │  └─────────┘ │              │
  │  │ summary │ │     └──────────────┘              │
  │  │  tools  │ │                                    │
  │  ├─────────┤ │     ┌──────────────┐              │
  │  │assistant│ │     │    acp/      │              │
  │  │  tools  │ │     │   manager    │              │
  │  └─────────┘ │     │              │              │
  └──────────────┘     │  ┌─────────┐ │              │
                       │  │discover │ │   UDP广播    │
  ┌──────────────┐     │  ├─────────┤ │   发现其他   │
  │   alarm/     │     │  │  group  │ │   Agent      │
  │   manager    │     │  └─────────┘ │              │
  │              │     └──────────────┘              │
  │  SQLite +    │                                    │
  │  Timer       │     ┌──────────────┐              │
  └──────────────┘     │  websocket/  │              │
                       │   manager    │◀─────────────┘
                       │              │
                       │  频道订阅    │
                       │  广播/单播   │
                       │  离线回调    │
                       └──────────────┘

  依赖关系说明：
  ─────▶  直接依赖
  memory → graph      记忆系统使用知识图谱存储
  memory → cache      记忆路由使用 LRU 缓存
  tools → plugins     工具注册表包含插件提供的工具
  tools → graph       图工具依赖图数据库
  context → session   上下文管理使用会话持久化
  websocket → session 连接离线时保存上下文到会话
  cxfc → plugins      CXFC 插件联邦与插件系统协作
```

---

## 5. 数据流图

### 5.1 对话流程

```
  用户输入（文本/语音）
       │
       ▼
  ┌──────────┐    WebSocket     ┌────────────────┐
  │ Frontend │ ──────────────▶ │  Gateway /ws   │
  │ ChatPage │  chat.stream    │                │
  └──────────┘                 └───────┬────────┘
                                       │
                                       ▼
                              ┌────────────────┐
                              │  Chat Handler  │
                              │  (handlers/    │
                              │   chat.py)     │
                              └───────┬────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                  │
                    ▼                 ▼                  ▼
            ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
            │ Agent Config │  │   Context    │  │   Memory     │
            │   加载       │  │   Manager    │  │   Router     │
            └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
                   │                 │                  │
                   └────────┬────────┘                  │
                            │                           │
                            ▼                           │
                   ┌────────────────┐                   │
                   │ Prompt Builder │◀──────────────────┘
                   │                │    记忆上下文注入
                   │ 1. system_prompt
                   │ 2. hidden_prompt（工具/情感/音效指令）
                   │ 3. memory_context（相关记忆）
                   │ 4. CXFC skill injection
                   │ 5. 对话历史
                   │ 6. 用户消息
                   └───────┬────────┘
                           │
                           ▼
                   ┌────────────────┐
                   │  Model Router  │
                   │  (main/summary/│
                   │    memory)     │
                   └───────┬────────┘
                           │
                           ▼
                   ┌────────────────┐
                   │  LLM (Ollama/  │
                   │      vLLM)     │
                   └───────┬────────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │  文本流   │ │Tool Calls│ │  思考流   │
        │  chunk   │ │ 工具调用  │ │ thinking │
        └────┬─────┘ └────┬─────┘ └──────────┘
             │             │
             │             ▼
             │      ┌──────────────┐
             │      │ Tools 执行   │
             │      │ (builtin/   │
             │      │  MCP/CXFC/  │
             │      │  custom)    │
             │      └──────┬───────┘
             │             │
             │             ▼
             │      ┌──────────────┐
             │      │  LLM 二次   │
             │      │  调用生成   │
             │      └──────┬───────┘
             │             │
             └──────┬──────┘
                    │
                    ▼
           ┌────────────────┐
           │ Emotion Parser │  解析情感标记
           │ Effect Parser  │  解析音效标记
           └───────┬────────┘
                   │
          ┌────────┼────────┐
          │        │        │
          ▼        ▼        ▼
   ┌──────────┐┌────────┐┌──────────┐
   │ 文本响应  ││TTS合成 ││ 情感/表情 │
   │ 流式推送  ││流式音频││ 指令推送  │
   └────┬─────┘└───┬────┘└────┬─────┘
        │          │          │
        └──────────┼──────────┘
                   │ WebSocket stream
                   ▼
           ┌──────────────┐
           │   Frontend   │
           │  ChatPage    │
           │  + Live2D/VRM│
           └──────────────┘
```

### 5.2 直播流程

```
  ┌──────────────────────────────────────────────────────────────┐
  │                        直播数据源                              │
  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
  │  │ 弹幕消息  │  │ 礼物事件  │  │ 进入房间  │  │ 外部事件  │    │
  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
  └───────┼─────────────┼─────────────┼─────────────┼───────────┘
          │             │             │             │
          └─────────────┴─────────────┴─────────────┘
                        │
                        ▼
               ┌────────────────┐
               │  Live Client   │
               │  Handler       │
               │ (live_client   │
               │  .py)          │
               └───────┬────────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
   ┌────────────┐ ┌──────────┐ ┌──────────┐
   │  Firewall  │ │  Prompt  │ │  Context │
   │  防火墙    │ │  Builder │ │  Manager │
   │ (3档过滤)  │ │  直播提示 │ │  直播上下 │
   └─────┬──────┘ └────┬─────┘ └────┬─────┘
         │              │            │
         └──────┬───────┴────────────┘
                │
                ▼
        ┌──────────────┐
        │     LLM      │
        │   生成回复    │
        └──────┬───────┘
               │
       ┌───────┼───────┐
       │       │       │
       ▼       ▼       ▼
  ┌────────┐┌──────┐┌──────────┐
  │ASR+VAD ││ TTS  ││ Emotion  │
  │语音输入 ││语音  ││ + Effect │
  │打断检测 ││合成  ││ 解析     │
  └───┬────┘└──┬───┘└────┬─────┘
      │        │         │
      └────────┼─────────┘
               │
               ▼
  ┌──────────────────────────────────────────────────┐
  │              Frontend LivePage                    │
  │  ┌────────────┐  ┌────────────┐  ┌────────────┐ │
  │  │  LiveStage  │  │ Danmaku    │  │ Subtitle   │ │
  │  │  虚拟形象   │  │ Overlay    │  │ Display    │ │
  │  │ Live2D/VRM │  │ 弹幕覆盖   │  │ 字幕显示   │ │
  │  └─────┬──────┘  └────────────┘  └────────────┘ │
  │        │                                         │
  │        ▼                                         │
  │  ┌────────────┐                                  │
  │  │ OBS 分层   │                                  │
  │  │ 输出       │                                  │
  │  │ - 形象层   │                                  │
  │  │ - 字幕层   │                                  │
  │  │ - 弹幕层   │                                  │
  │  └────────────┘                                  │
  └──────────────────────────────────────────────────┘
```

### 5.3 语音工作站流程

```
  ┌─────────────────────────────────────────────────────────────┐
  │              CX-O-VoiceWorkStation 五步工作流                │
  │                                                             │
  │  Step 1: 参考音频生成                                       │
  │  ┌─────────────────────────────────────────────────────┐   │
  │  │  VoxCPM                                              │   │
  │  │  ┌────────────┐ ┌──────────────┐ ┌──────────────┐  │   │
  │  │  │  design    │ │ controllable │ │  ultimate    │  │   │
  │  │  │  设计模式  │ │  _clone      │ │  _clone      │  │   │
  │  │  │  文本→语音 │ │  参考音+控制 │ │  音频克隆    │  │   │
  │  │  └────────────┘ └──────────────┘ └──────────────┘  │   │
  │  └──────────────────────────┬──────────────────────────┘   │
  │                             │ 输出: 基础参考音频            │
  │                             ▼                               │
  │  Step 2: 情感参考音频生成                                   │
  │  ┌─────────────────────────────────────────────────────┐   │
  │  │  CosyVoice                                           │   │
  │  │  ┌──────────────────┐  ┌──────────────────────────┐ │   │
  │  │  │ 8 种情感参考     │  │ 56 种过渡参考            │ │   │
  │  │  │ (happy/sad/angry │  │ (neutral→happy,          │ │   │
  │  │  │  /surprised/     │  │  happy→sad, ...)         │ │   │
  │  │  │  fearful/...)    │  │ 8×7=56 过渡              │ │   │
  │  │  └──────────────────┘  └──────────────────────────┘ │   │
  │  │           共 64 参考音频                               │   │
  │  └──────────────────────────┬──────────────────────────┘   │
  │                             │ 输出: 情感参考音频包          │
  │                             ▼                               │
  │  Step 3: 训练数据准备                                       │
  │  ┌─────────────────────────────────────────────────────┐   │
  │  │  So-VITS-SVC 预处理                                  │   │
  │  │  - 音频切片                                          │   │
  │  │  - 语音标注                                          │   │
  │  │  - 特征提取                                          │   │
  │  └──────────────────────────┬──────────────────────────┘   │
  │                             │ 输出: 训练数据集              │
  │                             ▼                               │
  │  Step 4: 模型训练                                           │
  │  ┌─────────────────────────────────────────────────────┐   │
  │  │  So-VITS-SVC 训练                                    │   │
  │  │  - 可配置 epochs/batch_size/lr                       │   │
  │  │  - 异步训练任务                                      │   │
  │  │  - 同时支持 F5-TTS 微调                              │   │
  │  └──────────────────────────┬──────────────────────────┘   │
  │                             │ 输出: 训练模型                │
  │                             ▼                               │
  │  Step 5: 推理                                               │
  │  ┌─────────────────────────────────────────────────────┐   │
  │  │  So-VITS-SVC 推理 + IndexTTS 2                      │   │
  │  │  - 音色转换推理                                      │   │
  │  │  - IndexTTS 2: 8 情感 × 5 强度 = 40 组合            │   │
  │  └──────────────────────────┬──────────────────────────┘   │
  │                             │                               │
  │                             ▼                               │
  │                    ┌────────────────┐                       │
  │                    │  输出语音模型   │                       │
  │                    │  供 CX-O-SERVER│                       │
  │                    │  TTS 服务使用  │                       │
  │                    └────────────────┘                       │
  └─────────────────────────────────────────────────────────────┘
```

---

## 6. WebSocket 通信架构

### 6.1 连接端点

```
  ┌──────────────┐                          ┌──────────────────┐
  │  Frontend    │                          │  CX-O-SERVER     │
  │              │     WebSocket             │                  │
  │  ChatPage ───┼──── /ws ────────────────▶│  websocket_      │
  │              │     (通用连接)             │  endpoint()      │
  │              │                          │                  │
  │  LivePage ───┼──── /ws/live ───────────▶│  live_websocket_ │
  │              │     (直播连接)             │  endpoint()      │
  └──────────────┘                          └──────────────────┘
```

### 6.2 消息路由机制

```
  客户端发送消息
       │
       ▼
  ┌──────────────────────────────────────────────────┐
  │              WebSocketManager                     │
  │                                                  │
  │  消息格式: { "action": "...", "data": {...} }    │
  │           或 { "type": "...", "data": {...} }    │
  │                                                  │
  │  路由策略:                                       │
  │  ┌────────────────────────────────────────────┐  │
  │  │  1. type == "ping"  →  handle_ping()      │  │
  │  │  2. action == "system.health"              │  │
  │  │     →  handle_system_health()              │  │
  │  │  3. action in _action_handlers             │  │
  │  │     →  _action_handlers[action]()          │  │
  │  │  4. type in message_handlers               │  │
  │  │     →  message_handlers[type]()            │  │
  │  │  5. 未匹配 → error response               │  │
  │  └────────────────────────────────────────────┘  │
  └──────────────────────────────────────────────────┘
```

### 6.3 Action 处理器映射

```
  ┌─────────────────────────────────────────────────────────────┐
  │                    Action → Handler 映射                     │
  │                                                             │
  │  Action 域          Action 示例              Handler        │
  │  ─────────────────  ──────────────           ────────       │
  │  chat.*             chat.message             chat           │
  │                     chat.stream                             │
  │                     chat.multimodal                         │
  │                                                             │
  │  memory.*           memory.list              memory         │
  │                     memory.create                           │
  │                     memory.search                           │
  │                                                             │
  │  tools.*            tools.list               tools          │
  │                     tools.call                              │
  │                                                             │
  │  plugin.*           plugin.register          plugin         │
  │                     plugin.heartbeat                        │
  │                                                             │
  │  acp.*              acp.connect              acp            │
  │                     acp.disconnect                          │
  │                                                             │
  │  mcp.*              mcp.connect              mcp            │
  │                     mcp.call                                │
  │                                                             │
  │  asr.*              asr.recognize            audio          │
  │                     asr.stream                              │
  │                                                             │
  │  tts.*              tts.synthesize           audio          │
  │                     tts.synthesize_stream                   │
  │                                                             │
  │  voice.*            voice.dual_stream        audio          │
  │                     voice.partial                           │
  │                     voice.tts_chunk                         │
  │                     voice.prefill_started                   │
  │                                                             │
  │  external_event.*   external_event           events         │
  │                     events.subscribe                        │
  │                     events.unsubscribe                      │
  │                                                             │
  │  config.*           config.get               config         │
  │                                                             │
  │  metrics.*          metrics.get              metrics        │
  │                                                             │
  │  system.*           system.health            system         │
  │                                                             │
  │  context.*          context.get              context        │
  │                                                             │
  │  danmaku.*          danmaku.list             danmaku        │
  │                                                             │
  │  events.*           events.subscribe         events         │
  └─────────────────────────────────────────────────────────────┘
```

### 6.4 频道订阅/发布模型

```
  ┌────────────────────────────────────────────────────────────┐
  │                  WebSocketManager                           │
  │                                                            │
  │  connections: { client_id → WebSocketConnection }          │
  │  channels:    { channel_name → Set<client_id> }            │
  │                                                            │
  │  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
  │  │ Client A │  │ Client B │  │ Client C │                │
  │  │ 订阅:    │  │ 订阅:    │  │ 订阅:    │                │
  │  │ - chat   │  │ - chat   │  │ - live   │                │
  │  │ - events │  │ - events │  │ - danmaku│                │
  │  └────┬─────┘  └────┬─────┘  └────┬─────┘                │
  │       │              │              │                      │
  │       ▼              ▼              ▼                      │
  │  ┌──────────────────────────────────────────┐              │
  │  │          Channel: "chat"                  │              │
  │  │          { A, B }                         │              │
  │  ├──────────────────────────────────────────┤              │
  │  │          Channel: "events"                │              │
  │  │          { A, B }                         │              │
  │  ├──────────────────────────────────────────┤              │
  │  │          Channel: "live"                  │              │
  │  │          { C }                            │              │
  │  ├──────────────────────────────────────────┤              │
  │  │          Channel: "danmaku"               │              │
  │  │          { C }                            │              │
  │  └──────────────────────────────────────────┘              │
  │                                                            │
  │  通信方式:                                                 │
  │  - send_to_client()   单播：发送给指定客户端               │
  │  - broadcast()        广播：发送给所有客户端               │
  │  - broadcast_to_channel() 频道广播：发送给频道内客户端     │
  │  - broadcast_external_event() 外部事件广播                 │
  └────────────────────────────────────────────────────────────┘
```

### 6.5 连接生命周期

```
  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
  │  连接    │────▶│  活跃    │────▶│  超时    │────▶│  离线    │
  │  建立    │     │  通信    │     │  检测    │     │  保存    │
  └─────────┘     └──────────┘     └──────────┘     └──────────┘
       │               │                │                │
       ▼               ▼                ▼                ▼
  ws.accept()    更新 last_     cleanup_loop()    offline_callback()
  分配 client_id  activity     30分钟无活动       保存上下文到
  发送 connected               → disconnect()     长期记忆
                                                  归档旧消息
```

---

## 7. 技术栈

| 层级 | 技术 |
|------|------|
| **前端框架** | React 18 + TypeScript + Electron |
| **前端构建** | Vite |
| **前端样式** | Tailwind CSS |
| **虚拟形象** | PIXI.js (Live2D) + Three.js (VRM) |
| **后端框架** | FastAPI (Python) |
| **ASGI 服务器** | Uvicorn |
| **LLM 后端** | Ollama / vLLM |
| **向量数据库** | Weaviate (含 Embedded 模式) / Milvus Lite / ChromaDB |
| **关系数据库** | SQLite |
| **语音识别** | SenseVoice (Embedded) / Remote |
| **语音合成** | F5-TTS (Embedded) / Remote / Orpheus TTS (vLLM + SNAC) |
| **语音转换** | So-VITS-SVC |
| **情感语音** | CosyVoice + IndexTTS 2 |
| **语音生成** | VoxCPM |
| **VAD** | Energy / WebRTC / Silero |
| **通信协议** | WebSocket + REST API |
| **插件协议** | CXFC (插件联邦) + MCP (Model Context Protocol) |
| **多智能体** | ACP (Agent Collaboration Protocol) |
| **国际化** | i18n (zh-CN / en-US) |
