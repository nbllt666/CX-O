{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cx-o/public/schema/emotion_instruction.schema.json",
  "title": "EmotionInstruction",
  "description": "LLM 生成的独立自然语言 TTS 情感/韵律指令契约。与 reply_text 分离；具备长度、置信度、敏感内容与非法字段校验；非法 JSON/超时/低置信度/服务不可用回退中性指令（neutral=true）。不允许产生或依赖 [emotion:*]/Orpheus XML 旧标签。源真理：spec unify-qwen3-tts-migration 冻结决策。",
  "type": "object",
  "required": ["text", "neutral"],
  "properties": {
    "text": { "type": "string", "minLength": 1, "maxLength": 200, "description": "Qwen3 适用的自然语言情感/韵律表达（如「用特别愤怒的语气说」）。" },
    "intensity": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.5 },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1, "default": 0.5 },
    "neutral": { "type": "boolean", "default": false, "description": "true 表示回退中性指令。" },
    "raw": { "type": "string", "description": "LLM 原始输出（调试/审计，不入 Provider 协议）。" },
    "source": { "type": "string", "enum": ["llm", "fallback", "legacy_migration"], "description": "指令来源；legacy_migration 表示旧标签在迁移边界转换所得。" }
  }
}